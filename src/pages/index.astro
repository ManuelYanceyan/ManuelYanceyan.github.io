---
import Layout from "../layouts/BaseLayout.astro";
---

<style>
  /* Fondo con animación neón */
  body {
    background: radial-gradient(circle at center, #050505 0%, #000010 100%);
    color: #e0e0ff;
    font-family: 'Orbitron', sans-serif;
    min-height: 100vh;
    overflow: hidden;
    position: relative;
  }

  /* Animación de borde de luces */
  .neon-border {
    position: fixed;
    top: 0; left: 0; right: 0; bottom: 0;
    border: 3px solid transparent;
    border-radius: 10px;
    animation: neonPulse 4s linear infinite;
    pointer-events: none;
  }

  @keyframes neonPulse {
    0% { box-shadow: 0 0 10px #00ffff, inset 0 0 10px #00ffff; }
    25% { box-shadow: 0 0 25px #00ff99, inset 0 0 25px #00ff99; }
    50% { box-shadow: 0 0 25px #ff00ff, inset 0 0 25px #ff00ff; }
    75% { box-shadow: 0 0 25px #ff0099, inset 0 0 25px #ff0099; }
    100% { box-shadow: 0 0 10px #00ffff, inset 0 0 10px #00ffff; }
  }

  .container {
    display: flex;
    flex-direction: column;
    align-items: center;
    padding: 60px 20px;
    text-align: center;
    z-index: 2;
    position: relative;
  }

  h1 {
    font-size: 2.5rem;
    text-shadow: 0 0 10px #00ffff;
    margin-bottom: 20px;
  }

  p {
    color: #b3b3ff;
    font-size: 1.2rem;
    max-width: 600px;
    margin-bottom: 40px;
  }

  .btn-neon {
    background: transparent;
    border: 2px solid #00ffff;
    color: #00ffff;
    padding: 12px 28px;
    border-radius: 10px;
    text-transform: uppercase;
    letter-spacing: 1.5px;
    cursor: pointer;
    transition: all 0.3s ease;
    text-decoration: none;
    font-weight: bold;
    font-size: 1rem;
    box-shadow: 0 0 10px #00ffff;
  }

  .btn-neon:hover {
    background: #00ffff;
    color: #000;
    box-shadow: 0 0 30px #00ffff, 0 0 60px #00ffff;
  }

  .card {
    background: rgba(20, 20, 40, 0.8);
    border: 1px solid #00ffff33;
    border-radius: 15px;
    padding: 25px;
    margin-top: 30px;
    width: 80%;
    max-width: 800px;
    box-shadow: 0 0 20px #00ffff33;
    transition: all 0.3s ease;
  }

  .card:hover {
    box-shadow: 0 0 30px #00ffff99, 0 0 60px #00ffff55;
    transform: scale(1.02);
  }
</style>

<Layout>
  <div class="neon-border"></div>

  <div class="container">
    <h1>🚀 Bienvenido al Portafolio de Manuel</h1>
    <p>
      Este es mi espacio digital de aprendizaje en desarrollo web, donde combino código, diseño y energía gamer 🔥
    </p>

    <a href="/semanas" class="btn-neon">Ver Semanas</a>

    <div class="card">
      <h2>💻 Proyectos Destacados</h2>
      <p>Explora mis creaciones, laboratorios y reportes de aprendizaje.</p>
      <a href="/projects" class="btn-neon">Ver projectos</a>
    </div>
  </div>
</Layout>
